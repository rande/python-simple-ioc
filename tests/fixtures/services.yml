parameters:
    foo.bar: argument 1

services:
    fake:
        class: tests.ioc.service.Fake
        arguments: 
            - "%foo.bar%"
        kargs:
            param: here a parameter

    foo:
        class: tests.ioc.service.Foo
        arguments: ["@fake"]
        kargs: {}
